syntax="proto3";

package pbapi;

//定义go_package,必须以斜杠开头
option go_package="/pbapi"; 

message Request{
    string message=1;
}

message Response{
    string message=1;
}

service EchoService{
    rpc Echo(Request) returns (Response);
}